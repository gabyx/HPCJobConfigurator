language: python
python:
  - "3.2"
  - "3.3"
  - "3.4"
# command to install dependencies
install:
  - pip install -r requirements.txt
  
# command to run tests
script:
  - chmod +x travis/run_tests.sh
  - travis/run_tests.sh
  
